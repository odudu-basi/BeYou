import Foundation

/// Copy this file to Secrets.swift and fill in your actual keys.
/// Secrets.swift is gitignored — never commit real keys.
enum Secrets {
    // MARK: - RevenueCat
    static let revenueCatAPIKey = "YOUR_REVENUECAT_API_KEY"

    // MARK: - Supabase
    static let supabaseURL = "https://YOUR_PROJECT.supabase.co"
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"

    // MARK: - Mixpanel
    static let mixpanelToken = "YOUR_MIXPANEL_TOKEN"

    // MARK: - TikTok
    static let tiktokAppID = "YOUR_TIKTOK_APP_ID"
    static let tiktokAppSecret = "YOUR_TIKTOK_APP_SECRET"
}
